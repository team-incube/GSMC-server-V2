spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  data:
    redis:
      host: redis
      port: 6379
  sql:
    init:
      mode: always
cloud:
  aws:
    s3:
      bucket: test-gsm-certification-system
    # 자격 증명은 모킹으로 처리, 실제 값 불필요
    credentials:
      access-key: dummy
      secret-key: dummy
    region:
      static: ap-northeast-2
server:
  port: 8080